# Staging web build — optional file on the BUILD machine.
#
# For same-origin hosting (Nginx serves / and proxies /api), the default build
# uses relative /api/... URLs — you usually do NOT need a .env for staging.
#
# If the API is on a different origin, set at build time:
# VITE_API_BASE_URL=https://api.staging.example.com
